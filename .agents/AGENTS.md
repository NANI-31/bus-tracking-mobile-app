# Project Rules

## Git Workflow Rules
- Do NOT run `git push` automatically.
- After implementing and verifying changes, stage/commit locally or provide the formatted commit message for the user to review.
